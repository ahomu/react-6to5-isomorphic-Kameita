/* global YourApp */
'use strict';

// for nodeJS
//import  * as assert from 'power-assert';

import foo from '../src/foo';

describe('test', function() {
  it('foo?',function(){
    assert(flow6to5IsomorphicKameita != null);
  });
  it('foo!',function(){
    assert(foo() == 'bar!');
  });
});
